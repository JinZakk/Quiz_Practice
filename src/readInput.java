import java.util.Scanner;
//When playing the game, a player is asked for the coordinates of the peice they would like to place
public class readInput {

	public static void main(String[] args) {
		// This is a test class to read in user input and place the input within a two dimensional array
		
		Scanner myScan = new Scanner(System.in);
		System.out.println("Enter Row[0-7]: ");
		int row = myScan.nextInt();
		System.out.println("Enter Column[0-7]: ");
		int column = myScan.nextInt();
		row = (row*2) + 1;
		column = 
		
		
	}

}
